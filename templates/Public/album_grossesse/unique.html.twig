{% extends 'base.html.twig' %}

{% block title %}Album grossesse{% endblock %}

{% block main %}
    <div class="container mt-4">
        <h1 class="mb-4 bordeaux">Album grossesse unique</h1>
        <div class="row flex">
            {% for grossesse in grossesses %}{#boucle pour afficher tous les produits du tableau#}
                <div class="col-sm-12 col-lg-6">
                    <div class="card mb-4">
                        {% if grossesse.media %}
                            <div>
                                <img class="resp" src="{{ asset('uploads/image_product/' ~ grossesse.media) }}" alt="{{ grossesse.name }}">
                            </div>
                        {% endif %}
                        <div class="card-body">
                            <h2 class="card-title">
                                <a class="bordeaux" href="{{ path('public/album_grossesse/show', {'id':grossesse.id}) }}">{{ grossesse.name }}</a>
                            </h2>
                            <div class="bordeaux">{{ grossesse.price }}€</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}{{ naissances.name }}{% endblock %}

{% block main %}

    <div class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <img class="resp" src="{{ asset('uploads/image_product/' ~ naissances.media) }}" alt="{{ naissances.name }}">
                </div>
                <div class="col-md-4">
                    <h1 class="bordeaux">{{ naissances.name }}</h1>
                    <div class="detail">{{ naissances.price }}€</div>
                    <a href="{{ path('contact') }}" class="btn btn-bordeaux">Me contacter</a>
                </div>
            </div>
        </div>
    </div>
    <div class="container mt-4">
        <h3 class="bordeaux">Description :</h3>
        <p>{{ naissances.description | nl2br }}</p>{#nl2br = filtre saut de ligne auto, si présent dans le contenu#}
    </div>

{% endblock %}

